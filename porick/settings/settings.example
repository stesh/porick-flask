DEBUG = False # MUST be False for production environments
SQLALCHEMY_DATABASE_URI = 'mysql+mysqldb://localhost/porick'
PASSWORD_SALT = 'A bcrypt password salt. Can be generated by doing "import bcrypt; bcrypt.gensalt()"'
COOKIE_SECRET = 'Another, different, random string, to be kept secret.'
